from common.models import Website, Trending_news

from django.http import HttpResponse,JsonResponse


def listwebsites(request): ## wait until redesign...
    # ERROR : Class has no objects member
    # In VS open the Command Palette Ctrl+Shift+P and type in one of the following commands:
    # Python: Select Linter
    # when you select a linter it will be installed.
    # return http resposen in Json formate
    retStr = ''

    return HttpResponse(retStr)



